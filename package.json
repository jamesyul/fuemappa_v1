{
  "name": "fuemappa-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install-all": "npm --prefix backend install && npm --prefix frontend install"
  }
}